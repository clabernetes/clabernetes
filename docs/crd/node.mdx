---
title: Node
description: Node custom resource schema.
icon: Server
---

The `Node` CRD represents a single containerlab node realized as one direct device Pod. Together
with `Link`, Nodes are the primary c9s API. The object name is the containerlab node name; the
namespace is the topology boundary.

<CrdViewer name="node" />
