# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# jetbrains goodies
.idea

# python
__pycache__/
*.pyc

# macos stuff
.DS_Store
*/.DS_Store

# private dir for notes and such
private/
.private/

# log output
*.log

# vscode
.vscode
.devcontainer

# coverage
cover.out

# pnpm cache
.pnpm-store/

# devspace cache
*.devspace
.develop/.registry-port-forward.*
.develop/.registry-deploy.lock
.develop/.registry-push.lock

# direnv file
.envrc

# goreleaser dist dir for testing builds and such locally
.dist/

# build files
cmd/clabverter/build/
# ignore generated try-c9s state (tools + per-cluster rendered manifests),
# but keep the manifest templates committed at build/try-c9s/*.yaml
build/try-c9s/*/
# ignore downloaded e2e tool binaries, but keep build/e2e/kind.yaml committed
build/e2e/bin/
# ignore the pinned DevSpace binary downloaded by `make dev`
build/dev/bin/

# for crd -> openapi gen
venv
