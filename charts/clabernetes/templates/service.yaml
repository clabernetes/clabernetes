---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-http
  namespace: {{ .Release.Namespace }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    revision: "{{ .Release.Revision }}"
    c9s.run/app: {{ .Values.appName }}
    c9s.run/name: "{{ .Values.appName }}-manager"
    c9s.run/component: manager-http
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 10443
  selector:
    c9s.run/app: {{ .Values.appName }}
    c9s.run/name: "{{ .Values.appName }}-manager"
    c9s.run/component: manager